#include <stdio.h>
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return(0);
}

/*
 * void means main function accepts no input
 * and the function returns zero 
 *  - because the type is int 
 *  - and when it's able to print successfully
 */
